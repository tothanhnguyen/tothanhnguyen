name: update-activity-panel

on:
  schedule:
    - cron: "17 17 * * *" # daily 00:17 ICT
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate activity panel SVGs
        run: python3 scripts/generate-activity-panel.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/activity-panel.svg
          git diff --cached --quiet || (git commit -m "chore: refresh activity panel" && git push)
